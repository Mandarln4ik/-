# NeuroForge v1.2.0

Генерация картинок исправлена по эталонной реализации самой модели, чаты, свои модели из
Hugging Face, текстовые модели на NPU, конвертация на самом телефоне и две новые проверки
в CI — обе поставлены из-за того, что ломалось раньше.

---

## Пайплайн генерации был неверен почти во всех параметрах

Я писал его по соглашениям семейства FLUX, а не по эталонному host-loop самой модели
(`generate.py` в релизе litert-samples). Почти каждый тензор, который уходил в графы, был
неправильным.

**Ничего из этого не падало.** Три графа фиксированной формы и целиком float: подайте
тензор правильной формы с неправильными числами — получите картинку правильного размера
с шумом. Именно поэтому я и запрашивал эталон.

| параметр | было | стало |
|---|---|---|
| каналов в латенте | 16 (FLUX) | **32** |
| токенов промпта | 77 (CLIP) | **256**, в chat-шаблоне, с маской внимания |
| time-shift | 3.0 захардкожен | **выводится** из числа токенов и шагов (≈7.62) |
| конец рампы сигм | 1/1000 | **1/steps** |
| timestep | sigma × 1000 | **sigma** |
| входов у DiT | 3 | **5** (lat, embeds, sigma, img_ids, txt_ids) |
| входов у текстового энкодера | 1 | **2** (input_ids, attention_mask) |
| пространство латента | паковался каждый шаг | **токенное** на всём цикле |
| аффинное преобразование латента | отсутствовало | **BatchNorm-статистики** из `pipeline_meta.json` |

Единственное, что совпало — порядок упаковки патчей. Теперь он проверяется напрямую против
индексной арифметики эталона, а не только против собственного round-trip.

Две вещи больше не угадываются, а выясняются при загрузке:

- **Куда класть каждый аргумент DiT.** Эталон прямо предупреждает: рантайм отдаёт буферы не
  в порядке аргументов. У всех пяти входов этого графа разное число элементов, поэтому
  привязка выводится однозначно из размеров буферов, а не из порядка.
- **Токенизация служебных меток.** `<|im_start|>` и остальные теперь идут как свои id, а не
  разбиваются BPE на обычную пунктуацию.

## Загрузки наконец работают

Все имена файлов в каталоге были **неверны** — и мои догадки, и подстраховка, искавшая
подстроку `int8` в файле с именем `...wi8_afp32`. CI теперь проверяет каждый URL на каждом
коммите и печатает реальное содержимое репозитория, когда имя не совпало. Так и нашлись:

| было | стало |
|---|---|
| `dit_int4.tflite` | `dit_int4b32.tflite` |
| `vae_decoder.tflite` | `vae_dec_fp32.tflite` |
| `text_encoder_int4.tflite` | `textenc_int4.tflite` |
| `mobilenet_v3_small_int8_channelwise.tflite` | `mobilenet_v3_small_weight_only_wi8_afp32.tflite` |

Размеры тоже убраны: они были из того же описания модели и так же не проверены. Тест падает,
если непроверенный размер снова станет условием загрузки.

## Одна кнопка на модель

Раньше каждый файл качался отдельно. Теперь карточка модели = одна кнопка, которая тянет всё:
веса, декодер, метаданные — с общим прогрессом и указанием, какой файл идёт сейчас.

## Свои модели из Hugging Face

Поле ввода репозитория → приложение показывает **реальный список файлов** с размерами →
скачиваете любой `.tflite` или `.litertlm`.

Это прямое следствие трёх поломок подряд: все они были из-за того, что имя файла писал я.
Теперь вас не касается, насколько хорошо я угадал.

## Вкладка «Чаты»

Разные типы, а не один «умный» чат, который угадывает:

| Тип | Состояние |
|---|---|
| **Текст** | Qwen3 0.6B сразу, Gemma 3 1B — после токена (см. ниже) |
| **Картинки** | диффузия + апскейл, результат прямо в переписке |
| **Музыка** | вкладка есть, но честно говорит, что модели нет |

Модель выбирается при создании чата и не меняется: ответы из разных моделей в одной ветке
невозможно интерпретировать. Под каждым ответом видно ускоритель и время.

Музыку я не стал прятать: вкладка показывает причину, а не ведёт в тупик после загрузки.

## Текстовые модели реально идут на NPU — и теперь под ваш чип

Проверка каталога показала то, чего я не ожидал: `litert-community` публикует сборки
**под конкретные ускорители**.

```
Gemma3-1B-IT_q4_ekv1280_mt6989.litertlm
Gemma3-1B-IT_q4_ekv1280_mt6991.litertlm   ← MT6991, ровно то, что сообщает ваш телефон
Gemma3-1B-IT_q4_ekv1280_sm8750.litertlm
Gemma3-1B-IT_q8_ekv1280_Google_Tensor_G5.litertlm
```

Это графы, уже скомпилированные под один APU. Приложение читает `Build.SOC_MODEL` и ставит
его первым в списке предпочтений при выборе файла — на телефоне, который сообщает **MT6991**,
скачается именно сборка MT6991, а не общая. Это самый близкий к настоящему ответ на «пусть
текстовая модель работает на NPU», который сегодня существует.

**Gemma закрыта лицензией.** CI нашёл файл ровно там, где написано в каталоге, и загрузка
всё равно отвечает 401: Google требует принять лицензию Gemma на странице модели. Это делает
человек в браузере, ретраем не лечится. Поэтому в настройках появилось поле для токена
Hugging Face, а сообщение об ошибке говорит, что именно делать.

**Qwen3 0.6B** — без всякой лицензии, 475 МиБ, имя файла закреплено из реального листинга:
`qwen3_0_6b_mixed_int4.litertlm`.

## Как это устроено внутри

LiteRT-LM — другой рантайм, он владеет KV-кэшем и циклом декодирования, поэтому чат нельзя
собрать из тех же деталей, что и картинки. Зато у него есть `Backend.NPU` — это
**единственное место в приложении, где генеративная модель действительно достаёт до APU**.
Диффузионный трансформер не может, а эта может.

## Конвертация на телефоне — я был неправ

Я говорил «на Android невозможно». Верно это было только для **внутри APK**. Через Termux
поднимается Debian aarch64 на тех же ARM64-ядрах, и весь тулчейн под ARM64 существует —
проверено скачиванием пакетов:

```
ai_edge_litert-2.2.0-cp311-cp311-manylinux_2_27_aarch64.whl
torch 2.14 → manylinux_2_28_aarch64
ai-edge-torch, ai-edge-quantizer → чистый Python
```

Никакой эмуляции. Инструкция — `docs/ON_DEVICE_CONVERSION.md`, скрипты — `tools/android/`.

Про подсистему Windows: WSL это компонент ядра Windows, на Android его нет, а эмуляция x86
на ARM замедлила бы в 10–50 раз. Нативные ARM-сборки уже есть — эмулировать нечего.

**Не проверено:** полный прогон конвертации на телефоне. ARM64-устройства у меня нет.
Упереться может в память (4B не влезет) или во время (часы).

## Две новые проверки в CI

**Проверка каталога** — теперь делает ровно то же, что делает приложение: пробует
объявленный URL, а если его нет — берёт листинг репозитория и выбирает файл той же
функцией `chooseModelFile`. Старая версия была **строже приложения** и валила записи,
которые на телефоне работали бы. Заодно печатает реальный размер каждого файла, чтобы его
можно было закрепить измеренным, а не списанным с описания модели.

Попутно исправлены два бага, которые эта проверка поймать не могла: запасной поиск всегда
искал `.tflite` независимо от расширения записи (переименованный `.litertlm` не находился
никогда), а листинг был не рекурсивным — токенизатор в своей папке был невидим.

**Smoke-тест запуска** — ставит APK на эмулятор, запускает и смотрит logcat и живость
процесса. Ловит падения вроде v1.1.0: ошибка порядка инициализации компилируется без единого
предупреждения, так что сборка APK о ней ничего не говорит — только запуск.

Сам тест с первого раза не заработал, и по интересной причине: экшен выполняет скрипт
**построчно**, каждую строку отдельным `sh -c`. Поэтому `set -o pipefail` был недопустимой
опцией, а многострочный `if ... fi` — синтаксической ошибкой на первой же строке. Оба раза
шаг падал **до установки APK**, и в логе это выглядело ровно как падение при запуске.

Проверка, которая не может пройти, хуже отсутствующей — её не отличить от той поломки, ради
которой она стоит. Теперь проверки лежат в `.github/scripts/smoke.sh`, а в workflow одна
строка, которая его вызывает.

---

## Проверено

- **136 юнит-тестов** ядра, 0 падений. Новые закрепляют лестницу сигм по эталонным
  значениям, геометрию латента, позиционные id, аффинное преобразование, chat-шаблон и
  привязку входов графа.
- **Распаковка латента сверена с numpy** — прогон `unpatchify()` из `generate.py` на реальной
  форме (1024×128 → 1×32×64×64), десять точечных значений плюс сумма, максимум и минимум.
  Транспонирование, сдвиг на элемент и перестановка аффинного с распаковкой ловятся все три.
- Каждая запись каталога разрешается в реальный файл в репозитории.
- APK собирается и **запускается** на эмуляторе.

**Не проверено:** сама генерация картинки на устройстве. ARM64-телефона здесь нет, а графы
весят около 4 ГБ. Контракт тензоров теперь сверен с эталоном построчно, но это не то же
самое, что увиденная картинка.

## Артефакты

- `app-debug.apk` — подписан debug-ключом, ставится поверх.
- `app-release-unsigned.apk` — после R8, без подписи.
